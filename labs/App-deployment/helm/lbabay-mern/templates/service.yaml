apiVersion: v1
kind: Service
metadata:
  name: {{ include "lbabay-mern.fullname" . }}
  labels:
    {{- include "lbabay-mern.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lbabay-mern.selectorLabels" . | nindent 4 }}
